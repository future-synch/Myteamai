from typing import Any


def find_matching_listings(criteria: dict, limit: int = 3) -> list:
    """
    Stub for FS-16 real matching engine.
    Returns empty list until Listings schema migration
    (FS-64) and Olesya field decisions (FS-63) are resolved.
    """
    return []
